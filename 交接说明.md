# 交接说明（配合新 plan 使用）

> 本文件 + 新计划书（`ops-agent fullstack plan`）两份，构成给"新 Agent"的完整交接。
> 新 Agent 请按此顺序开始：
> 1. 读本文件，理解**背景、目标、取舍依据、环境事实、执行前清理**；
> 2. 读计划书（执行依据，**勿编辑**），按 M0→M10 从零实现；
> 3. 动手前**先做第 5 节的清理**，再从空目录开始。
>
> 计划书当前位置：`/Users/mayihao/.cursor/plans/ops-agent_fullstack_plan_743d8446.plan.md`（名为 "ops-agent fullstack plan"）。

---

## 1. 求职者背景与目标（项目存在的原因）

- **真实经历**：1.5 年安卓客户端 + 2 年 Java/Go 后端 + 0.5 年解决方案；目前已离职约 8 个月。
- **学历/公司**：哈工大本科 + 北理工硕士（双 985/211，电子通信）；唯一雇主华为（华为云/终端，2021.08–2025.09）。核心加分项。
- **包装口径**：对外按"3 年 Java 后端"呈现；华为离职证明 title 为"软件开发工程师"，因此**全程开发叙事是安全的**。
- **终极目标**：拿到 **Agent 开发岗** offer。
- **已确认的关键选择**：
  1. 定位 = **懂 Agent 的后端工程师**（后端为基底 + Agent 为亮点），不走纯 Agent/算法路线。
  2. 时间线 = **1 个月内开始投递**（边投边继续加深项目）。
  3. Agent 项目方向 = **运维/平台诊断+操作 Agent**（结合其 K8s/日志背景）。
- **真实弱点（项目需对冲）**：无真正高并发、无 JVM 调优、无丰富 DB 读写；无 Agent 实战、无 Python 经验；8 个月 gap 需"主动技术转型"叙事覆盖（由本 Agent 项目承担）。

## 2. 这个项目要证明什么（指导一切取舍）

这是一份**面试作品**，不是生产系统。它要同时证明三件事，缺一不可：
1. **Agent 工程能力**：编排、RAG、记忆、人审、评测、可观测。
2. **后端↔Agent 集成能力**：Agent 接入自研 Spring Boot 后端（适配层 + 统一契约 + mock/real 对等）。
3. **云原生部署与运维能力**：Docker / docker-compose / K8s（探针/HPA/ConfigMap/Secret）、健康检查与指标。

> 取舍原则：**Agent 主体（计划书 M0–M7）是核心**，Java 后端与部署（M8–M10）是体现"后端工程师"身份的叠加层。任何阶段都必须保持 **mock-first：零外部依赖（无需后端、API Key、Docker）即可离线 demo 与跑测**。

贯穿全项目、必须 mock 与真实后端两侧一致的三个故障场景：
- lts-access：限流阈值误配，日志被丢到 retry-topic，接入 QPS 骤降。
- bcs-agent：数据面升级到坏镜像导致 CrashLoop，0/3 副本就绪。
- lts-config：某日志流被人工暂停，导致无数据流入。

**面试弹药**（必须落到代码与 README，便于答"遇到什么困难、怎么解决"）：幻觉抑制(RAG+结构化+引用)、工具调用不稳(Pydantic 校验+失败回灌+重试)、上下文超长(压缩)、延迟成本(缓存+模型分级+并行)、安全(HITL+风险分级+审计)、评测难(自建集+LLM-as-judge+回归)；以及后端集成与上云运维这条全链路。

## 3. 仓库真实项目映射（背景理解，无需改动这些目录）

仓库根：`/Users/mayihao/1.code/project-for-interview/ops-agent/`（Git monorepo，含 Python Agent、Simulator、Java 后端与 deploy）。以下为求职者真实项目的本地复刻，**仅供理解业务语境**；新 plan 用的是**自研的 `ops-backend`**，不强依赖这些旧服务：

- `rpc-agent`（Spring Boot）+ `enhanced-api` → 简历"节点引擎服务"（Redis+Lua 限流、Gas 优化）
- `lts-access` + `lts-config`（Spring Boot）→ 简历"云日志服务"（接入/Kafka/限流/重试、生命周期/路由）
- `bcs-manager` + `bcs-agent`（Go/Beego/K8s）→ 简历"区块链服务"（控制面/数据面分离、CA/成员/合约、ExecutionMode 安全分级）
- 三个故障场景即取材于上述系统，使作品与简历强关联。

## 4. 相关文件

- **新计划书**（执行依据，勿编辑）：`ops-agent fullstack plan`（路径见文首）。
- **简历**：`简历-后端Agent方向.md`——内有"运维诊断与操作 Agent"项目段，含 `[待填]` 量化指标，待计划书 M7/M10 完成后回填。
- **本交接说明**：`交接说明.md`。

## 5. 执行前清理（动手第一步，重要）

求职者已决定**完全弃用旧目录与旧计划**，从零重建。新 Agent 开工前请先删除：
- 旧目录 `ops-agent/`（含旧 `app/` 与旧 `.venv`，整目录删除）。
- 旧计划 `ops-agent_build_plan_0569b9b9.plan.md`（位于仓库根目录）。

删除后，严格按新计划书第 2 节的目标目录结构，从空白开始创建 `ops-agent/`、`ops-backend/`、`deploy/`。

## 6. 环境事实（已核实，写入约束）

- **Python**：用 3.12（`/opt/homebrew/bin/python3.12`，实测 3.12.10）。在 `ops-agent/` 下新建 venv：`python3.12 -m venv .venv`，依赖用 `.venv/bin/python -m pip install`。`requires-python = ">=3.11"`，Docker 镜像用 `python:3.12-slim`。
- **Java**：本机活动 JDK 为 **Java 8**，Maven 3.9.9 可用。`ops-backend` 目标 **Spring Boot 3 / Java 17**，**用 Docker 多阶段（`maven:3.9-eclipse-temurin-17`）容器内构建**以规避 host JDK8；本地 `mvn` 运行需 JDK17（IntelliJ 自带可用）。
- **Docker**：28 可用（compose 正常）。**无 kubectl**：K8s manifests 作为交付物编写，用 `kubectl --dry-run=client` 校验语法即可，实际应用需 kind/minikube/CCE。
- **网络**：允许列表放行 PyPI 与 Maven Central；首次构建镜像需联网拉取。pip/uv 安装个别包可能较慢，留足超时。
- **默认运行配置**：`BACKEND_MODE=mock`、`LLM_MODE=mock`、`EMBEDDINGS_PROVIDER=local-hash`、`CHECKPOINTER=sqlite`。

## 7. 执行纪律（与计划书一致，便于自检）

- 从零创建，不依赖任何既有 ops-agent 文件（旧目录已在第 5 节删除）。
- 每个里程碑补对应测试并自检；**始终保持 mock-first 离线可跑**。
- Java/部署为叠加层：即使后续中断，M0–M7 也应是一个可独立运行、可演示的完整 Agent。
- 依赖装最新稳定版，**禁止臆造版本号**；不把真实密钥写进仓库（K8s Secret 用占位模板）。
- **不编辑计划书文件**；完成 M7/M10 后把量化指标与部署事实回填 `简历-后端Agent方向.md`。
